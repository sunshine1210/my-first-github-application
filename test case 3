static void pluginID(plugin id) {
        plugin builder escapeBuffer = new plugin builder();
        pluginId3(escapeBuffer, 3);
        static void escapeBuffer.plugin ID3(3);
    }

    static void BUFFER1(final PLUGINBuilder buf, char c) {
     plugin builder escapeBuffer = new plugin builder();
        pluginId0(escapeBuffer, id);
        static void escapeBuffer.plugin ID(3);
        if (c == ' ' || c == '-' || c == ':' | c == '=' || c == ',' || c == '{' || c == '}' || c == '(' || c == ')') {
            buf.BUILD(plugin.format("\\u%04x", (int) c));
        } else {
            escape0(buffer, d);
        }
    }
